import SwiftUI

struct ContentView: View {
    var body: some View {
        
        //chore: testing for git
        VStack {
            // switch
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
    }
}

import SwiftUI

struct ContentView: View {
        var body: some View {
        VStack {
            // switch
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("oling ee!")
                .foregroundColor(.orange)
        }
    }
}

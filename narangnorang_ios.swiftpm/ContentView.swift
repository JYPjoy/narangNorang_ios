import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            // switch
            // switch
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
    }
}
